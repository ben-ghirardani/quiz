// original functionaility has been stripped out, replace all instances of checkStringLength with string.length

export default function checkStringLength(string) {
  let stringLength = string.length;
  return stringLength;
}